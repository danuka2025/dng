import { Code, Palette, Database, Layout, Smartphone, Globe, Server, Settings } from "lucide-react"

export const iconComponents = {
  Code,
  Palette,
  Database,
  Layout,
  Smartphone,
  Globe,
  Server,
  Settings,
}

export type IconName = keyof typeof iconComponents
